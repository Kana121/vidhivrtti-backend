server:
  port: 9090

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Kana121/configuration-properties.git
          clone-on-start: true
management:
  security:
    enabled: false
